abstract class {{feature_name.pascalCase()}}DataSource {
  // Add your data source methods here
  // Example:
  Future<Map<String, dynamic>> fetch{{feature_name.pascalCase()}}s();
  
  // Uncomment and implement the method below if needed
  // Future<ResponseModel> get{{feature_name.pascalCase()}}(id);

  // Uncomment and implement the method below if needed
  // Future<ResponseModel> create{{feature_name.pascalCase()}}({{feature_name.pascalCase()}}Model {{feature_name.camelCase()}});
  
  // Uncomment and implement the method below if needed
  // Future<ResponseModel> update{{feature_name.pascalCase()}}({{feature_name.pascalCase()}}Model {{feature_name.camelCase()}});
  
  // Uncomment and implement the method below if needed
  // Future<ResponseModel> delete{{feature_name.pascalCase()}}(id);
  
}
